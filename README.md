# nifsi
A distributed userspace operating system layer that unifies multiple Linux machines into a single system. Main goal is to achive this without using YAML or containers or orchestration ceremony.
